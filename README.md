# data-engineering-zoomcamp
The 'pipeline' folder contains a full pipeline from the first lesson as well as new service in the docker-compose.yaml file.
The 'homework' folder contains connect-test.py which reads local .csv and .parquet (in .gitignore) files and then writes them in the postgres db.
